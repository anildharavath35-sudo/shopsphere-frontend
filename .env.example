# Public Razorpay Key ID - safe to expose to the browser.
# It is loaded at runtime to initialise the Razorpay checkout widget.
# Leave blank while using PAYMENT_DEV_MODE=true on the server.
VITE_RAZORPAY_KEY_ID=